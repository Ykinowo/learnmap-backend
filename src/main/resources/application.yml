upload:
  path: D:\LearnMap\LearnMapUploads   # 请修改为你电脑上实际存在的目录，例如 C:/uploads 或 /Users/你的用户名/uploads

spring:
  web:
    resources:
      static-locations: file:${upload.path},classpath:/static/
  servlet:
    multipart:
      max-file-size: 10MB      # 限制上传文件大小
      max-request-size: 10MB